import SwiftUI

extension Color {
    // MARK: - Message Bubbles

    static let messageBubbleUser = Color("MessageBubbleUser")
    static let messageBubbleAssistant = Color("MessageBubbleAssistant")

    // MARK: - Connection Status

    static let connectionStatusGreen = Color("ConnectionGreen")
    static let connectionStatusRed = Color("ConnectionRed")
    static let connectionStatusOrange = Color("StatusOrange")

    // MARK: - Content Blocks

    static let toolUse = Color("ToolUse")
    static let toolResultSuccess = Color("ToolResultSuccess")
    static let toolResultError = Color("ToolResultError")
    static let thinking = Color("Thinking")

    // MARK: - Role Indicators

    static let userRole = Color("UserRole")
    static let assistantRole = Color("AssistantRole")

    // MARK: - Code Display

    static let codeBlockBackground = Color("CodeBlockBackground")
}
